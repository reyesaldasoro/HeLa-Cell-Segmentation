<?xml version="1.0" encoding="utf-8"?>
<mscript xmlns:mwsh="http://www.mathworks.com/namespace/mcode/v1/syntaxhighlight.dtd">
   <version>9.2</version>
   <release>2017a</release>
   <date>2018-07-03</date>
   <cell>
      <count>1</count>
      <steptitle>Read ROIs from The Crick</steptitle>
      <mcode>dir0                = dir('*.tiff');</mcode>
      <mcode-xmlized>
         <mwsh:code xml:space="preserve">dir0                = dir(<mwsh:strings xml:space="preserve">'*.tiff'</mwsh:strings>);</mwsh:code></mcode-xmlized>
      <mcode-count>1</mcode-count>
      <cellOutputTarget>1</cellOutputTarget>
   </cell>
   <cell>
      <count>2</count>
      <steptitle>display 1 slice of 1 data set</steptitle>
      <mcode>currentSet          = 12;
currentName         = dir0(currentSet).name;
currentSetInfo      = imfinfo(dir0(currentSet).name);
numSlices           = size(currentSetInfo,1);

filtG               = gaussF(5,5,1);</mcode>
      <mcode-xmlized>
         <mwsh:code xml:space="preserve">currentSet          = 12;
currentName         = dir0(currentSet).name;
currentSetInfo      = imfinfo(dir0(currentSet).name);
numSlices           = size(currentSetInfo,1);

filtG               = gaussF(5,5,1);</mwsh:code></mcode-xmlized>
      <mcode-count>2</mcode-count>
      <cellOutputTarget>2</cellOutputTarget>
   </cell>
   <cell>
      <count>3</count>
      <mcode>%currentSlice=242;
centralSlice        = round(numSlices/2);
currentImage        = imread(dir0(currentSet).name,centralSlice);
figure(1)
imagesc(imfilter(currentImage,filtG))
title(strcat(dir0(currentSet).name,'  (',num2str(currentSet),')    ','   -  ',num2str(centralSlice),'/',num2str(numSlices)),'interpreter','none')
colormap gray
Hela_nuclei(:,:,centralSlice) = segmentNucleiHelaEM(currentImage);
Hela_background(:,:,centralSlice) = segmentBackgroundHelaEM(currentImage);

figure(2)
imagesc( Hela_nuclei(:,:,centralSlice));

colormap gray</mcode>
      <mcode-xmlized>
         <mwsh:code xml:space="preserve"><mwsh:comments xml:space="preserve">%currentSlice=242;</mwsh:comments>
centralSlice        = round(numSlices/2);
currentImage        = imread(dir0(currentSet).name,centralSlice);
figure(1)
imagesc(imfilter(currentImage,filtG))
title(strcat(dir0(currentSet).name,<mwsh:strings xml:space="preserve">'  ('</mwsh:strings>,num2str(currentSet),<mwsh:strings xml:space="preserve">')    '</mwsh:strings>,<mwsh:strings xml:space="preserve">'   -  '</mwsh:strings>,num2str(centralSlice),<mwsh:strings xml:space="preserve">'/'</mwsh:strings>,num2str(numSlices)),<mwsh:strings xml:space="preserve">'interpreter'</mwsh:strings>,<mwsh:strings xml:space="preserve">'none'</mwsh:strings>)
colormap <mwsh:strings xml:space="preserve">gray</mwsh:strings>
Hela_nuclei(:,:,centralSlice) = segmentNucleiHelaEM(currentImage);
Hela_background(:,:,centralSlice) = segmentBackgroundHelaEM(currentImage);

figure(2)
imagesc( Hela_nuclei(:,:,centralSlice));

colormap <mwsh:strings xml:space="preserve">gray</mwsh:strings></mwsh:code></mcode-xmlized>
      <mcode-count>3</mcode-count>
      <cellOutputTarget>3</cellOutputTarget>
      <img src="testGit_01.png"/>
      <img src="testGit_02.png"/>
   </cell>
   <originalCode>%% Read ROIs from The Crick



dir0                = dir('*.tiff');

%% display 1 slice of 1 data set


currentSet          = 12;
currentName         = dir0(currentSet).name;
currentSetInfo      = imfinfo(dir0(currentSet).name);
numSlices           = size(currentSetInfo,1);

filtG               = gaussF(5,5,1);
%%
%currentSlice=242;
centralSlice        = round(numSlices/2);
currentImage        = imread(dir0(currentSet).name,centralSlice);
figure(1)
imagesc(imfilter(currentImage,filtG))
title(strcat(dir0(currentSet).name,'  (',num2str(currentSet),')    ','   -  ',num2str(centralSlice),'/',num2str(numSlices)),'interpreter','none')
colormap gray
Hela_nuclei(:,:,centralSlice) = segmentNucleiHelaEM(currentImage);    
Hela_background(:,:,centralSlice) = segmentBackgroundHelaEM(currentImage);
 
figure(2)
imagesc( Hela_nuclei(:,:,centralSlice));

colormap gray</originalCode>
   <m-file>testGit</m-file>
   <filename>/Volumes/CITY/Crick/ROIs/testGit.m</filename>
   <outputdir>/Volumes/CITY/Crick/ROIs/html</outputdir>
</mscript>